# Dependencies
highlighter:      rouge

# Permalinks
permalink:        pretty

# Setup
title:            Ĝi estas Blogo
baseurl:          /

exclude: [ _locale , _scripts , "*.backup" ]

url: "https://xxx.github.io"

plugins:
  - jekyll-relative-links
  - jekyll-sitemap

relative_links:
  enabled:     true
  collections: true
